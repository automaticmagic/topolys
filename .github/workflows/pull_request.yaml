name: Pull Request CI

on:
  pull_request:
    branches:
      - master
      
jobs:
  test:
    runs-on: ubuntu-18.04
    steps:
    - name: Check out repository
      uses: actions/checkout@v2    
    - name: Run Tests
      run: |
        bundle update
        bundle exec rake